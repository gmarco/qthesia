
FILE (GLOB VPIANO_SRC *.cpp)
INCLUDE(${QT_USE_FILE})
SET(QtApp_RCCS pianokeybd.qrc)
QT4_ADD_RESOURCES(QtApp_RCC_SRCS ${QtApp_RCCS})
QT4_AUTOMOC(PIANO_MOC_SRCS vpiano.cpp )

ADD_LIBRARY(piano STATIC ${VPIANO_SRC} ${QtApp_RCC_SRCS} ${PIANO_MOC_SRCS})
#TARGET_LINK_LIBRARIES(piano ${QT_LIBRARIES} )
